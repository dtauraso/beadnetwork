#!/usr/bin/env bash

# PLACEMENT: none | universal prose hygiene: comments are removed everywhere, not in one place
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(git rev-parse --show-toplevel)"
cd "$REPO_ROOT"

APPLY=0
[ "${1:-}" = "--apply" ] && APPLY=1

readonly KEEP='go:generate|nolint|lint:ignore|eslint-|@ts-|prettier-|tslint:|@type|@param|@returns|PLACEMENT:|_START|_END|generated by|GENERATED by|@generated|DO NOT EDIT|Regenerate:|FINGERPRINT|Source:|Copyright|SPDX|LICEN[SC]E|[0-9]{4}-[0-9]{2}-[0-9]{2}|path-resolution-ok|deliberate:|allowlist'

readonly STRIP_BLOCKS='
  function flush(keep,   i) {
    if (emit) for (i = 1; i <= n; i++) if (keep) print buf[i]
    if (!keep) removed += n
    n = 0
  }
  /^[[:space:]]*\/\/.*_START/ { fenced = 1 }
  /^[[:space:]]*\/\// {
    buf[++n] = $0
    if (fenced || $0 ~ keep) blockKeep = 1
    if ($0 ~ /_END/) fenced = 0
    next
  }
  {
    if (n) { flush(blockKeep); dropped = !blockKeep; blockKeep = 0 }
    if (dropped && $0 ~ /^[[:space:]]*$/) { dropped = 0; next }
    dropped = 0
    if (emit) print
  }
  END { if (n) flush(blockKeep); if (!emit) print removed + 0 }
'

files=()
while IFS= read -r f; do
  case "$f" in */node_modules/*) continue ;; esac
  head -3 "$f" 2>/dev/null | grep -qiE 'generated by|@generated' && continue
  files+=("$f")
done < <(git ls-files '*.go' '*.ts' '*.tsx')

total=0
for f in "${files[@]}"; do
  n="$(awk -v keep="$KEEP" -v emit=0 "$STRIP_BLOCKS" "$f")"
  [ "$n" -eq 0 ] && continue
  total=$((total + n))
  if [ "$APPLY" -eq 1 ]; then
    awk -v keep="$KEEP" -v emit=1 "$STRIP_BLOCKS" "$f" > "$f.stripped" && mv "$f.stripped" "$f"
  else
    printf '%6d  %s\n' "$n" "$f"
  fi
done

if [ "$APPLY" -eq 1 ]; then
  echo "strip-comments: removed $total comment line(s) from ${#files[@]} file(s)"
else
  echo "strip-comments: $total comment line(s) in ${#files[@]} file(s) would be removed (--apply to do it)"
fi
