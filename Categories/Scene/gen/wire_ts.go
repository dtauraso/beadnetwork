package main

import (
	"fmt"
	"os"
	"path/filepath"
	"strings"

	"github.com/dtauraso/wirefold/Categories/Input/Stdin"
	"github.com/dtauraso/wirefold/Categories/Scene"
)

func writeWireTS(srcRoot string) {
	var b strings.Builder
	fmt.Fprintln(&b, "// GENERATED by Categories/Scene/gen — do not edit.")
	fmt.Fprintln(&b, "// Source: Scene.UpdateAttrs and Stdin's record_kinds.go.")
	fmt.Fprintln(&b, "// Regenerate with: go generate ./...")
	fmt.Fprintln(&b)
	fmt.Fprintf(&b, "export const IN_KIND_EDIT_UPDATE = %d;\n\n", Stdin.InKindEditUpdate)
	writeWireList(&b, "IN_UPDATE_ATTRS", Scene.UpdateAttrs)
	fmt.Fprintln(&b)
	fmt.Fprintln(&b, "// EDIT_UPDATE_KINDS_START")
	writeWireList(&b, "IN_UPDATE_KINDS", Stdin.InUpdateKinds)
	fmt.Fprintln(&b, "// EDIT_UPDATE_KINDS_END")
	fmt.Fprintln(&b)

	outPath := filepath.Join(srcRoot, "Scene", "wire-gen.ts")
	if err := os.WriteFile(outPath, []byte(b.String()), 0o644); err != nil {
		fatalf("write %s: %v", outPath, err)
	}
	announce(outPath, len(Scene.UpdateAttrs), "update attrs")
}

func writeWireList(b *strings.Builder, name string, items []string) {
	fmt.Fprintf(b, "export const %s = [", name)
	for i, v := range items {
		if i > 0 {
			b.WriteString(", ")
		}
		fmt.Fprintf(b, "%q", v)
	}
	fmt.Fprintln(b, "] as const;")
}
