// GENERATED by Categories/NodeKinds/NodePhiTheta/portsdef — do not edit.
// Source: this kind's own SPEC.md ## View, ## Ports and ## Description.
// Regenerate with: go generate ./...

import type { NodeDef } from "../node-def";

// What this kind looks like in the editor, and the id it carries on the wire.
// node-defs.ts collects this fragment together with the other kinds'; the id
// is authored in SPEC.md and never renumbered, so a removed kind leaves a gap.
export const KIND_NAME = "NodePhiTheta";
export const KIND_ID = 12;
export const DEF: NodeDef = { bg: "#e8f5e9", border: "#2e7d32", text: "#1b3c1e", minWidth: 70, shape: "rect", fill: "#e8f5e9", stroke: "#2e7d32", width: 70, height: 60, desc: "One half of a φ, θ pair: adds each angle's offset to that angle of its center, wrapping at the quarter turn, and sends the stepped center on as the partner's next arrival." };
