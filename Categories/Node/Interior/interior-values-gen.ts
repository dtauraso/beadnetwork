// GENERATED by Categories/Node/Interior/gen from InteriorValueNames in interior_values.go —
// do not edit.
//
// One block file per node, because one goroutine per node writes it. The row
// sits in the path, not in the file, so no writer touches another node's bytes.

export const INTERIOR_VALUE_NAMES = [
  "present",
  "value",
  "x",
  "y",
  "z",
] as const;

export type InteriorValueName = (typeof INTERIOR_VALUE_NAMES)[number];

// How many interior slots a node has. Declared once, as SlotsPerNode in
// emitter.go, because it is the interior's own number and nothing else's.
export const INTERIOR_SLOTS_PER_NODE = 4;
