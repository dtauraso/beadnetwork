% update-arith-distance.tex — one block of the update. Shared setup is pairmath.sty,
% which also carries the rebuild command.
\documentclass[12pt]{article}
\usepackage{pairmath}
\begin{document}
\[\begin{array}{@{}l@{}}
  \text{\color{acc}distance} \\[4pt]
  \quad L(x,y) = \min\bigl(\lvert x-y \rvert,\; 24 - \lvert x-y \rvert\bigr) \\[3pt]
  \quad {\color{dim}\text{the word metric on } \mathbb{Z}_{24}} \\[8pt]
  \text{\color{acc}what arrives} \quad a = p + 6 \\[3pt]
  \quad {\color{dim}\text{the partner's normal}}
\end{array}\]
\end{document}
