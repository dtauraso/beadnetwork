% update-decide-turn.tex — one block of the update. Shared setup is pairmath.sty,
% which also carries the rebuild command.
\documentclass[12pt]{article}
\usepackage{pairmath}
\begin{document}
\[\begin{array}{@{}l@{}}
  \text{\color{acc}measure}
    \quad {\color{dim}\text{once, at the end the arrival is nearer}} \\[5pt]
  \quad u = (t - a) \bmod 24 \\[4pt]
  \quad c = u \;\text{ if } u < 12, \;\text{ else } (b - a) \bmod 24 \\[4pt]
  \quad {\color{dim}e = t \text{ if } u < 12, \text{ else } b
    \quad\text{--- the end that was counted from}} \\[9pt]
  \text{\color{acc}the count each mode stops at} \\[5pt]
  \quad {\color{par}\text{parallel} : \; s = 6} \\[4pt]
  \quad {\color{perp}\text{perpendicular} : \; s = 0} \\[4pt]
  \quad {\color{dim}\text{setting} : \; \text{every } c} \\[11pt]
  \text{\color{acc}then decide}
    \quad {\color{dim}\text{no distance, and nothing measured at the neighbours}} \\[5pt]
  \quad c = s : \qquad\qquad\qquad\; e_{\mathrm{after}} = e \\[5pt]
  \quad (s - c) \bmod 12 \le 6 : \quad e_{\mathrm{after}} = e_{+1} \\[5pt]
  \quad \text{else} : \qquad\qquad\qquad\;\, e_{\mathrm{after}} = e_{-1}
\end{array}\]
\end{document}
