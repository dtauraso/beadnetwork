% update-length.tex — one block of the update. Shared setup is pairmath.sty,
% which also carries the rebuild command.
\documentclass[12pt]{article}
\usepackage{pairmath}
\begin{document}
\[\begin{array}{@{}l@{}}
  d = \max(t,a) - \min(t,a) \\[6pt]
  L \;=\; \len(t,a) = \min(d,\; 24 - d)
\end{array}\]
\end{document}
