% update-questions.tex — one block of the update. Shared setup is pairmath.sty,
% which also carries the rebuild command.
\documentclass[12pt]{article}
\usepackage{pairmath}
\begin{document}

\[
\begin{aligned}
  \text{\color{acc}settle?} \quad &L \in R \\[6pt]
  \text{\color{acc}up?} \quad &\len(t+1,a) \text{ closer to } R \text{ than } \len(t-1,a)
\end{aligned}
\]
\end{document}
