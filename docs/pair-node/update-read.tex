% update-read.tex — one block of the update. Shared setup is pairmath.sty,
% which also carries the rebuild command.
\documentclass[12pt]{article}
\usepackage{pairmath}
\begin{document}

\[
\begin{aligned}
  \mathrm{normal}(t) &= t+6 \\[2pt]
  \mathrm{bottom}(t) &= t+12 \\[2pt]
  \mathrm{sends}     &= \mathrm{normal}(t) \\[2pt]
  \mathrm{angle}(i)  &= \tfrac{\pi}{12}\, i
\end{aligned}
\]
\end{document}
