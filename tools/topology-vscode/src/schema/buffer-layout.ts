// GENERATED by tools/gen-node-defs — do not edit.
// Source: Buffer/layout.go  Regenerate: npm run gen:node-defs
// BUF_LAYOUT_FINGERPRINT: version:35|interiorSlotsPerNode:4|block:Node[CX:f32:0,CY:f32:4,CZ:f32:8,Radius:f32:12,SphereR:f32:16,VRX:f32:20,VRY:f32:24,VRZ:f32:28,FRX:f32:32,FRY:f32:36,FRZ:f32:40,Selected:u8:44,KindId:u8:45,LabelOff:u32:46,LabelLen:u32:50,Hovered:u8:54,LatchedSel:u8:55,GotDragMsg:u8:56,DragDeltaA:i32:57,DragDeltaB:i32:61,DragDeltaC:i32:65,DragRequantCount:i32:69,GotForwardMsg:u8:73,ForwardDeltaA:i32:74,ForwardDeltaB:i32:78,ForwardDeltaC:i32:82,ForwardFromRow:i32:86,CascadeRelay:u8:90]:stride:91|block:ChainBead[OX:f32:0,OY:f32:4,OZ:f32:8,Lit:u8:12,LitValue:i32:13]:stride:17|block:Interior[Present:u8:0,Value:i32:1,OX:f32:5,OY:f32:9,OZ:f32:13]:stride:17|block:Edge[SX:f32:0,SY:f32:4,SZ:f32:8,EX:f32:12,EY:f32:16,EZ:f32:20,Selected:u8:24,EdgeLabelOff:u32:25,EdgeLabelLen:u32:29]:stride:33|block:LayoutLink[SrcNodeRow:i32:0,DstNodeRow:i32:4]:stride:8|block:Camera[PX:f32:0,PY:f32:4,PZ:f32:8,R:f32:12,PosTheta:f32:16,PosPhi:f32:20,UpTheta:f32:24,UpPhi:f32:28]:stride:32|block:Overlay[SceneTori:u8:0,ScenePoles:u8:1,NodePoles:u8:2,SelSpherePoles:u8:3,Handholds:u8:4,LabelsGlobal:u8:5,OverlaysVis:u8:6,CascadeLinks:u8:7,DragNodeRow:i32:8,GroupLenTime:f32:12,GroupLenInput:f32:16,GroupLenGate:f32:20]:stride:24|block:Scene[CX:f32:0,CY:f32:4,CZ:f32:8,Radius:f32:12]:stride:16|block:Event[Kind:u8:0,NodeRow:i32:1,PortRow:i32:5,TargetRow:i32:9,TargetPortRow:i32:13,EdgeRow:i32:17,Slot:i32:21,Value:i32:25,Bead:u32:29,BeadSteps:f32:33,SimLatencyMs:f32:37,X:f32:41,Y:f32:45,Z:f32:49,F:f32:53,Label:u8:57,Debug:u8:58,TextOff:u32:59,TextLen:u32:63]:stride:67

/** Schema version — must match BufLayoutVersion in Buffer/layout.go. */
export const BUF_LAYOUT_VERSION = 35;

/** Fixed interior-grid slots per node — must match BufInteriorSlotsPerNode in Buffer/layout.go.
 * Generated (part of BUF_LAYOUT_FINGERPRINT): a mismatch fails check-buffer-layout-parity.sh. */
export const INTERIOR_SLOTS_PER_NODE = 4;

/** Byte width of the retired combined-snapshot frame's header. The combined SCENE/BEAD/
 * NODE/EDGE frame is gone entirely — each goroutine now streams its own per-owner frame
 * (VIEW/EDGE_STREAM/NODE_STREAM/INTERIOR_STREAM — see frame-tags.ts and
 * memory/feedback_no_single_writer_bridge.md); this constant is a historical layout
 * marker, not consumed by any live decoder: [tick:u32][layoutLinkCount:u32] */
export const BUF_HEADER_SIZE = 8;

// ── Node block ───────────────────────────────────────────────
export const NODE_COL_CX                         = 0; // f32
export const NODE_COL_CY                         = 4; // f32
export const NODE_COL_CZ                         = 8; // f32
export const NODE_COL_RADIUS                     = 12; // f32
export const NODE_COL_SPHERE_R                   = 16; // f32
export const NODE_COL_VRX                        = 20; // f32
export const NODE_COL_VRY                        = 24; // f32
export const NODE_COL_VRZ                        = 28; // f32
export const NODE_COL_FRX                        = 32; // f32
export const NODE_COL_FRY                        = 36; // f32
export const NODE_COL_FRZ                        = 40; // f32
export const NODE_COL_SELECTED                   = 44; // u8
export const NODE_COL_KIND_ID                    = 45; // u8
export const NODE_COL_LABEL_OFF                  = 46; // u32
export const NODE_COL_LABEL_LEN                  = 50; // u32
export const NODE_COL_HOVERED                    = 54; // u8
export const NODE_COL_LATCHED_SEL                = 55; // u8
export const NODE_COL_GOT_DRAG_MSG               = 56; // u8
export const NODE_COL_DRAG_DELTA_A               = 57; // i32
export const NODE_COL_DRAG_DELTA_B               = 61; // i32
export const NODE_COL_DRAG_DELTA_C               = 65; // i32
export const NODE_COL_DRAG_REQUANT_COUNT         = 69; // i32
export const NODE_COL_GOT_FORWARD_MSG            = 73; // u8
export const NODE_COL_FORWARD_DELTA_A            = 74; // i32
export const NODE_COL_FORWARD_DELTA_B            = 78; // i32
export const NODE_COL_FORWARD_DELTA_C            = 82; // i32
export const NODE_COL_FORWARD_FROM_ROW           = 86; // i32
export const NODE_COL_CASCADE_RELAY              = 90; // u8
export const NODE_STRIDE                         = 91;

export function readNodeCX(view: DataView, row: number): number { return view.getFloat32(row * NODE_STRIDE + NODE_COL_CX, true); }
export function readNodeCY(view: DataView, row: number): number { return view.getFloat32(row * NODE_STRIDE + NODE_COL_CY, true); }
export function readNodeCZ(view: DataView, row: number): number { return view.getFloat32(row * NODE_STRIDE + NODE_COL_CZ, true); }
export function readNodeRadius(view: DataView, row: number): number { return view.getFloat32(row * NODE_STRIDE + NODE_COL_RADIUS, true); }
export function readNodeSphereR(view: DataView, row: number): number { return view.getFloat32(row * NODE_STRIDE + NODE_COL_SPHERE_R, true); }
export function readNodeVRX(view: DataView, row: number): number { return view.getFloat32(row * NODE_STRIDE + NODE_COL_VRX, true); }
export function readNodeVRY(view: DataView, row: number): number { return view.getFloat32(row * NODE_STRIDE + NODE_COL_VRY, true); }
export function readNodeVRZ(view: DataView, row: number): number { return view.getFloat32(row * NODE_STRIDE + NODE_COL_VRZ, true); }
export function readNodeFRX(view: DataView, row: number): number { return view.getFloat32(row * NODE_STRIDE + NODE_COL_FRX, true); }
export function readNodeFRY(view: DataView, row: number): number { return view.getFloat32(row * NODE_STRIDE + NODE_COL_FRY, true); }
export function readNodeFRZ(view: DataView, row: number): number { return view.getFloat32(row * NODE_STRIDE + NODE_COL_FRZ, true); }
export function readNodeSelected(view: DataView, row: number): number { return view.getUint8(row * NODE_STRIDE + NODE_COL_SELECTED); }
export function readNodeKindId(view: DataView, row: number): number { return view.getUint8(row * NODE_STRIDE + NODE_COL_KIND_ID); }
export function readNodeLabelOff(view: DataView, row: number): number { return view.getUint32(row * NODE_STRIDE + NODE_COL_LABEL_OFF, true); }
export function readNodeLabelLen(view: DataView, row: number): number { return view.getUint32(row * NODE_STRIDE + NODE_COL_LABEL_LEN, true); }
export function readNodeHovered(view: DataView, row: number): number { return view.getUint8(row * NODE_STRIDE + NODE_COL_HOVERED); }
export function readNodeLatchedSel(view: DataView, row: number): number { return view.getUint8(row * NODE_STRIDE + NODE_COL_LATCHED_SEL); }
export function readNodeGotDragMsg(view: DataView, row: number): number { return view.getUint8(row * NODE_STRIDE + NODE_COL_GOT_DRAG_MSG); }
export function readNodeDragDeltaA(view: DataView, row: number): number { return view.getInt32(row * NODE_STRIDE + NODE_COL_DRAG_DELTA_A, true); }
export function readNodeDragDeltaB(view: DataView, row: number): number { return view.getInt32(row * NODE_STRIDE + NODE_COL_DRAG_DELTA_B, true); }
export function readNodeDragDeltaC(view: DataView, row: number): number { return view.getInt32(row * NODE_STRIDE + NODE_COL_DRAG_DELTA_C, true); }
export function readNodeDragRequantCount(view: DataView, row: number): number { return view.getInt32(row * NODE_STRIDE + NODE_COL_DRAG_REQUANT_COUNT, true); }
export function readNodeGotForwardMsg(view: DataView, row: number): number { return view.getUint8(row * NODE_STRIDE + NODE_COL_GOT_FORWARD_MSG); }
export function readNodeForwardDeltaA(view: DataView, row: number): number { return view.getInt32(row * NODE_STRIDE + NODE_COL_FORWARD_DELTA_A, true); }
export function readNodeForwardDeltaB(view: DataView, row: number): number { return view.getInt32(row * NODE_STRIDE + NODE_COL_FORWARD_DELTA_B, true); }
export function readNodeForwardDeltaC(view: DataView, row: number): number { return view.getInt32(row * NODE_STRIDE + NODE_COL_FORWARD_DELTA_C, true); }
export function readNodeForwardFromRow(view: DataView, row: number): number { return view.getInt32(row * NODE_STRIDE + NODE_COL_FORWARD_FROM_ROW, true); }
export function readNodeCascadeRelay(view: DataView, row: number): number { return view.getUint8(row * NODE_STRIDE + NODE_COL_CASCADE_RELAY); }

// ── ChainBead block ──────────────────────────────────────────
export const CHAIN_BEAD_COL_OX                   = 0; // f32
export const CHAIN_BEAD_COL_OY                   = 4; // f32
export const CHAIN_BEAD_COL_OZ                   = 8; // f32
export const CHAIN_BEAD_COL_LIT                  = 12; // u8
export const CHAIN_BEAD_COL_LIT_VALUE            = 13; // i32
export const CHAIN_BEAD_STRIDE                   = 17;

export function readChainBeadOX(view: DataView, row: number): number { return view.getFloat32(row * CHAIN_BEAD_STRIDE + CHAIN_BEAD_COL_OX, true); }
export function readChainBeadOY(view: DataView, row: number): number { return view.getFloat32(row * CHAIN_BEAD_STRIDE + CHAIN_BEAD_COL_OY, true); }
export function readChainBeadOZ(view: DataView, row: number): number { return view.getFloat32(row * CHAIN_BEAD_STRIDE + CHAIN_BEAD_COL_OZ, true); }
export function readChainBeadLit(view: DataView, row: number): number { return view.getUint8(row * CHAIN_BEAD_STRIDE + CHAIN_BEAD_COL_LIT); }
export function readChainBeadLitValue(view: DataView, row: number): number { return view.getInt32(row * CHAIN_BEAD_STRIDE + CHAIN_BEAD_COL_LIT_VALUE, true); }

// ── Interior block ───────────────────────────────────────────
export const INTERIOR_COL_PRESENT                = 0; // u8
export const INTERIOR_COL_VALUE                  = 1; // i32
export const INTERIOR_COL_OX                     = 5; // f32
export const INTERIOR_COL_OY                     = 9; // f32
export const INTERIOR_COL_OZ                     = 13; // f32
export const INTERIOR_STRIDE                     = 17;

export function readInteriorPresent(view: DataView, row: number): number { return view.getUint8(row * INTERIOR_STRIDE + INTERIOR_COL_PRESENT); }
export function readInteriorValue(view: DataView, row: number): number { return view.getInt32(row * INTERIOR_STRIDE + INTERIOR_COL_VALUE, true); }
export function readInteriorOX(view: DataView, row: number): number { return view.getFloat32(row * INTERIOR_STRIDE + INTERIOR_COL_OX, true); }
export function readInteriorOY(view: DataView, row: number): number { return view.getFloat32(row * INTERIOR_STRIDE + INTERIOR_COL_OY, true); }
export function readInteriorOZ(view: DataView, row: number): number { return view.getFloat32(row * INTERIOR_STRIDE + INTERIOR_COL_OZ, true); }

// ── Edge block ───────────────────────────────────────────────
export const EDGE_COL_SX                         = 0; // f32
export const EDGE_COL_SY                         = 4; // f32
export const EDGE_COL_SZ                         = 8; // f32
export const EDGE_COL_EX                         = 12; // f32
export const EDGE_COL_EY                         = 16; // f32
export const EDGE_COL_EZ                         = 20; // f32
export const EDGE_COL_SELECTED                   = 24; // u8
export const EDGE_COL_EDGE_LABEL_OFF             = 25; // u32
export const EDGE_COL_EDGE_LABEL_LEN             = 29; // u32
export const EDGE_STRIDE                         = 33;

export function readEdgeSX(view: DataView, row: number): number { return view.getFloat32(row * EDGE_STRIDE + EDGE_COL_SX, true); }
export function readEdgeSY(view: DataView, row: number): number { return view.getFloat32(row * EDGE_STRIDE + EDGE_COL_SY, true); }
export function readEdgeSZ(view: DataView, row: number): number { return view.getFloat32(row * EDGE_STRIDE + EDGE_COL_SZ, true); }
export function readEdgeEX(view: DataView, row: number): number { return view.getFloat32(row * EDGE_STRIDE + EDGE_COL_EX, true); }
export function readEdgeEY(view: DataView, row: number): number { return view.getFloat32(row * EDGE_STRIDE + EDGE_COL_EY, true); }
export function readEdgeEZ(view: DataView, row: number): number { return view.getFloat32(row * EDGE_STRIDE + EDGE_COL_EZ, true); }
export function readEdgeSelected(view: DataView, row: number): number { return view.getUint8(row * EDGE_STRIDE + EDGE_COL_SELECTED); }
export function readEdgeEdgeLabelOff(view: DataView, row: number): number { return view.getUint32(row * EDGE_STRIDE + EDGE_COL_EDGE_LABEL_OFF, true); }
export function readEdgeEdgeLabelLen(view: DataView, row: number): number { return view.getUint32(row * EDGE_STRIDE + EDGE_COL_EDGE_LABEL_LEN, true); }

// ── LayoutLink block ─────────────────────────────────────────
export const LAYOUT_LINK_COL_SRC_NODE_ROW        = 0; // i32
export const LAYOUT_LINK_COL_DST_NODE_ROW        = 4; // i32
export const LAYOUT_LINK_STRIDE                  = 8;

export function readLayoutLinkSrcNodeRow(view: DataView, row: number): number { return view.getInt32(row * LAYOUT_LINK_STRIDE + LAYOUT_LINK_COL_SRC_NODE_ROW, true); }
export function readLayoutLinkDstNodeRow(view: DataView, row: number): number { return view.getInt32(row * LAYOUT_LINK_STRIDE + LAYOUT_LINK_COL_DST_NODE_ROW, true); }

// ── Camera block ─────────────────────────────────────────────
export const CAMERA_COL_PX                       = 0; // f32
export const CAMERA_COL_PY                       = 4; // f32
export const CAMERA_COL_PZ                       = 8; // f32
export const CAMERA_COL_R                        = 12; // f32
export const CAMERA_COL_POS_THETA                = 16; // f32
export const CAMERA_COL_POS_PHI                  = 20; // f32
export const CAMERA_COL_UP_THETA                 = 24; // f32
export const CAMERA_COL_UP_PHI                   = 28; // f32
export const CAMERA_STRIDE                       = 32;

export function readCameraPX(view: DataView): number { return view.getFloat32(CAMERA_COL_PX, true); }
export function readCameraPY(view: DataView): number { return view.getFloat32(CAMERA_COL_PY, true); }
export function readCameraPZ(view: DataView): number { return view.getFloat32(CAMERA_COL_PZ, true); }
export function readCameraR(view: DataView): number { return view.getFloat32(CAMERA_COL_R, true); }
export function readCameraPosTheta(view: DataView): number { return view.getFloat32(CAMERA_COL_POS_THETA, true); }
export function readCameraPosPhi(view: DataView): number { return view.getFloat32(CAMERA_COL_POS_PHI, true); }
export function readCameraUpTheta(view: DataView): number { return view.getFloat32(CAMERA_COL_UP_THETA, true); }
export function readCameraUpPhi(view: DataView): number { return view.getFloat32(CAMERA_COL_UP_PHI, true); }

// ── Overlay block ────────────────────────────────────────────
export const OVERLAY_COL_SCENE_TORI              = 0; // u8
export const OVERLAY_COL_SCENE_POLES             = 1; // u8
export const OVERLAY_COL_NODE_POLES              = 2; // u8
export const OVERLAY_COL_SEL_SPHERE_POLES        = 3; // u8
export const OVERLAY_COL_HANDHOLDS               = 4; // u8
export const OVERLAY_COL_LABELS_GLOBAL           = 5; // u8
export const OVERLAY_COL_OVERLAYS_VIS            = 6; // u8
export const OVERLAY_COL_CASCADE_LINKS           = 7; // u8
export const OVERLAY_COL_DRAG_NODE_ROW           = 8; // i32
export const OVERLAY_COL_GROUP_LEN_TIME          = 12; // f32
export const OVERLAY_COL_GROUP_LEN_INPUT         = 16; // f32
export const OVERLAY_COL_GROUP_LEN_GATE          = 20; // f32
export const OVERLAY_STRIDE                      = 24;

export function readOverlaySceneTori(view: DataView): number { return view.getUint8(OVERLAY_COL_SCENE_TORI); }
export function readOverlayScenePoles(view: DataView): number { return view.getUint8(OVERLAY_COL_SCENE_POLES); }
export function readOverlayNodePoles(view: DataView): number { return view.getUint8(OVERLAY_COL_NODE_POLES); }
export function readOverlaySelSpherePoles(view: DataView): number { return view.getUint8(OVERLAY_COL_SEL_SPHERE_POLES); }
export function readOverlayHandholds(view: DataView): number { return view.getUint8(OVERLAY_COL_HANDHOLDS); }
export function readOverlayLabelsGlobal(view: DataView): number { return view.getUint8(OVERLAY_COL_LABELS_GLOBAL); }
export function readOverlayOverlaysVis(view: DataView): number { return view.getUint8(OVERLAY_COL_OVERLAYS_VIS); }
export function readOverlayCascadeLinks(view: DataView): number { return view.getUint8(OVERLAY_COL_CASCADE_LINKS); }
export function readOverlayDragNodeRow(view: DataView): number { return view.getInt32(OVERLAY_COL_DRAG_NODE_ROW, true); }
export function readOverlayGroupLenTime(view: DataView): number { return view.getFloat32(OVERLAY_COL_GROUP_LEN_TIME, true); }
export function readOverlayGroupLenInput(view: DataView): number { return view.getFloat32(OVERLAY_COL_GROUP_LEN_INPUT, true); }
export function readOverlayGroupLenGate(view: DataView): number { return view.getFloat32(OVERLAY_COL_GROUP_LEN_GATE, true); }

// ── Scene block ──────────────────────────────────────────────
export const SCENE_COL_CX                        = 0; // f32
export const SCENE_COL_CY                        = 4; // f32
export const SCENE_COL_CZ                        = 8; // f32
export const SCENE_COL_RADIUS                    = 12; // f32
export const SCENE_STRIDE                        = 16;

export function readSceneCX(view: DataView): number { return view.getFloat32(SCENE_COL_CX, true); }
export function readSceneCY(view: DataView): number { return view.getFloat32(SCENE_COL_CY, true); }
export function readSceneCZ(view: DataView): number { return view.getFloat32(SCENE_COL_CZ, true); }
export function readSceneRadius(view: DataView): number { return view.getFloat32(SCENE_COL_RADIUS, true); }

// ── Event block ──────────────────────────────────────────────
export const EVENT_COL_KIND                      = 0; // u8
export const EVENT_COL_NODE_ROW                  = 1; // i32
export const EVENT_COL_PORT_ROW                  = 5; // i32
export const EVENT_COL_TARGET_ROW                = 9; // i32
export const EVENT_COL_TARGET_PORT_ROW           = 13; // i32
export const EVENT_COL_EDGE_ROW                  = 17; // i32
export const EVENT_COL_SLOT                      = 21; // i32
export const EVENT_COL_VALUE                     = 25; // i32
export const EVENT_COL_BEAD                      = 29; // u32
export const EVENT_COL_BEAD_STEPS                = 33; // f32
export const EVENT_COL_SIM_LATENCY_MS            = 37; // f32
export const EVENT_COL_X                         = 41; // f32
export const EVENT_COL_Y                         = 45; // f32
export const EVENT_COL_Z                         = 49; // f32
export const EVENT_COL_F                         = 53; // f32
export const EVENT_COL_LABEL                     = 57; // u8
export const EVENT_COL_DEBUG                     = 58; // u8
export const EVENT_COL_TEXT_OFF                  = 59; // u32
export const EVENT_COL_TEXT_LEN                  = 63; // u32
export const EVENT_STRIDE                        = 67;

export function readEventKind(view: DataView, row: number): number { return view.getUint8(row * EVENT_STRIDE + EVENT_COL_KIND); }
export function readEventNodeRow(view: DataView, row: number): number { return view.getInt32(row * EVENT_STRIDE + EVENT_COL_NODE_ROW, true); }
export function readEventPortRow(view: DataView, row: number): number { return view.getInt32(row * EVENT_STRIDE + EVENT_COL_PORT_ROW, true); }
export function readEventTargetRow(view: DataView, row: number): number { return view.getInt32(row * EVENT_STRIDE + EVENT_COL_TARGET_ROW, true); }
export function readEventTargetPortRow(view: DataView, row: number): number { return view.getInt32(row * EVENT_STRIDE + EVENT_COL_TARGET_PORT_ROW, true); }
export function readEventEdgeRow(view: DataView, row: number): number { return view.getInt32(row * EVENT_STRIDE + EVENT_COL_EDGE_ROW, true); }
export function readEventSlot(view: DataView, row: number): number { return view.getInt32(row * EVENT_STRIDE + EVENT_COL_SLOT, true); }
export function readEventValue(view: DataView, row: number): number { return view.getInt32(row * EVENT_STRIDE + EVENT_COL_VALUE, true); }
export function readEventBead(view: DataView, row: number): number { return view.getUint32(row * EVENT_STRIDE + EVENT_COL_BEAD, true); }
export function readEventBeadSteps(view: DataView, row: number): number { return view.getFloat32(row * EVENT_STRIDE + EVENT_COL_BEAD_STEPS, true); }
export function readEventSimLatencyMs(view: DataView, row: number): number { return view.getFloat32(row * EVENT_STRIDE + EVENT_COL_SIM_LATENCY_MS, true); }
export function readEventX(view: DataView, row: number): number { return view.getFloat32(row * EVENT_STRIDE + EVENT_COL_X, true); }
export function readEventY(view: DataView, row: number): number { return view.getFloat32(row * EVENT_STRIDE + EVENT_COL_Y, true); }
export function readEventZ(view: DataView, row: number): number { return view.getFloat32(row * EVENT_STRIDE + EVENT_COL_Z, true); }
export function readEventF(view: DataView, row: number): number { return view.getFloat32(row * EVENT_STRIDE + EVENT_COL_F, true); }
export function readEventLabel(view: DataView, row: number): number { return view.getUint8(row * EVENT_STRIDE + EVENT_COL_LABEL); }
export function readEventDebug(view: DataView, row: number): number { return view.getUint8(row * EVENT_STRIDE + EVENT_COL_DEBUG); }
export function readEventTextOff(view: DataView, row: number): number { return view.getUint32(row * EVENT_STRIDE + EVENT_COL_TEXT_OFF, true); }
export function readEventTextLen(view: DataView, row: number): number { return view.getUint32(row * EVENT_STRIDE + EVENT_COL_TEXT_LEN, true); }

/** Sentinel Node KindId value meaning "unknown kind" (matches KindIDUnknown in Buffer/node_kind_id_gen.go). */
export const UNKNOWN_KIND_ID = 0xff;
