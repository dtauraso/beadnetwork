package gesture

import wire "github.com/dtauraso/wirefold/nodes/wire"

// vec3 is a local shorthand alias for wire.Vec3, matching package dispatch's own
// vec_alias.go — a fresh alias declaration in this package, not a re-export shim.
type vec3 = wire.Vec3
