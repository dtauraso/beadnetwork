package moverreg

import wire "github.com/dtauraso/wirefold/nodes/wire"

// vec3 is a local, unexported alias — same pattern as nodeactor/vec_alias.go and
// dispatch/vec_alias.go, so this file's signatures read the same as the ones it moved from.
type vec3 = wire.Vec3
