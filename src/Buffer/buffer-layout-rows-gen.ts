// GENERATED by tools/gen-node-defs — do not edit.
// Source: Buffer/bufschema/layout.go  Regenerate: npm run gen:node-defs

// This half holds the Recv, Fire block(s). Row blocks are split across two files
// purely to stay under check-file-size.sh's limit — the division is by position in
// Buffer/bufschema/layout.go, not by meaning, so neither file is a category.

// ── Recv block ───────────────────────────────────────────────
export const RECV_COL_NODE_ROW                   = 0; // i32
export const RECV_COL_VALUE                      = 4; // i32
export const RECV_STRIDE                         = 8;

export function readRecvNodeRow(view: DataView, row: number): number { return view.getInt32(row * RECV_STRIDE + RECV_COL_NODE_ROW, true); }
export function readRecvValue(view: DataView, row: number): number { return view.getInt32(row * RECV_STRIDE + RECV_COL_VALUE, true); }

// ── Fire block ───────────────────────────────────────────────
export const FIRE_COL_NODE_ROW                   = 0; // i32
export const FIRE_STRIDE                         = 4;

export function readFireNodeRow(view: DataView, row: number): number { return view.getInt32(row * FIRE_STRIDE + FIRE_COL_NODE_ROW, true); }

